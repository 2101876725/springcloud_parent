server:
  port: 8011
spring:
  application:
    name: provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
springdoc:
  api-docs:
    enabled: true
    path: /swagger-ui/api-docs
  swagger-ui:
    path: /swagger-ui/index.html
  packages-to-scan: com.cuit.provider